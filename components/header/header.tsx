import React from "react";
import { SafeAreaView, View } from "react-native";

export const Header = () => {
    return (
        <View>
            <SafeAreaView>
            </SafeAreaView>
        </View>
    );
}